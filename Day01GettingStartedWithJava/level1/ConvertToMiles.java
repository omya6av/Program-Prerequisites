class ConvertToMiles {
    public static void main(String args[]) {

        // Declare and assign variable for distance in kilometers
        double distance = 10.8;

        // Convert the distance kilometers to miles
        double miles = distance; 

        // Print the average marks
        System.out.println("The distance " + distance + " km in miles is = " + miles);

    }
}
